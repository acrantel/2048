class Tile {
  
  private Tile(int value, color c) {
    // private so that there is only one tile of value 2, 4, 8, 16, etc. instead of creating multiple tiles of value 2 that do exactly the same thing
  }
  public int getValue() {
  }
  public color getColor() {
  }
  /** Returns an instance of a Tile with the given value */
  public static Tile getTile(int value) {
  }
}
